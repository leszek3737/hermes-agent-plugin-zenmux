name: zenmux
version: 1.0.0
description: "ZenMux video generation backend (Vertex-style async predict). Text-to-video and image-to-video across Veo / Seedance via one key."
author: Leszek
kind: backend
requires_env:
  - ZENMUX_API_KEY
